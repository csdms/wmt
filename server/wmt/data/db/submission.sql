create table submission (
  id            integer primary key,
  uuid          text,
  name          text,
  status        text,
  created       text,
  updated       text,
  message       text,
  owner         text
);
